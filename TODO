# TODO/FIXME
#
# FIXME: in debian install live-config-systemd in jessie to not remove systemd; by default live-config installs live-config-sysvinit.
# FIXME: when chrooting to install packages always run prepare_chroot() and release_chroot() to avoid /dev/pts errors.
# FIXME: when building live change $dir/hostname and $dir/hosts from system hostname to "debian" and "ubuntu", respectively.
# FIXME: before rebuilding iso: rm -rf filesystem.squashfs before creating new one; possibly update vmlinuz and initrd if kernel was updated?
# IMPROV: use for chroots calls >> su -l -c "export LC_ALL=C ; export LANGUAGE=C ; export LANG=C ; $SUPPLIED_COMMAND".
#create functions run_system("ls $dir-binary"); and run_chroot($dir,"apt-get update");
# FIXME: GUI: rename "rebuild iso" to "build iso" and "build iso" to "prepare iso" and swap buttons. disable "prepare iso" if a $dir-binary already exists. Maybe rearrange Unsquash tab and Build tab to one and move "Build Live ISO" and "Backup" to "Chroot" tab.
# IMPROV: GUI: set default debian release to "jessie" and default ubuntu release to "xenial".
# IMPROV: GUI: in update tab add a button to clean apt. Add same button in Install tab.
# IMPROV: GUI: in install tab add a button to choose a script/hook to do automated stuff.
# IMPROV: GUI: enable "--no-install-recommends" by default; user unchek if wanted.
# IMPROV: GUI: sources.list help should be in a textbox, copy/pastable.
# IMPROV: GUI: add a port text input for "Chroot RDP".
# FIXME:
#Object::connect: No such signal QLineEdit::textChanged()
#Object::connect:  (sender name:   'lineEditISOPath')
# IMPROV: copyright/license.
# IMPROV: debian packaging.
# IMPROV: build USB img.
# IMPROV: support UEFI.
# FIXME: in ubuntu checksum always fails for one file. which one? why?
# IMPROV: upgrade livecd button: unsquash; remove casper from chroot to update initramfs; run dist-upgrade to update kernel; update vmlinuz and initrd.lz; reinstall casper in chroot.
# IMPROV: build a livecd with this program to create livecd of installed systems to another disk, like clonezilla/systemrescuecd/gparted.
#
#
# DEPENDS (replace with temp_pkg_system installs)
#
# xterm (no copy/paste?)
# rsync
